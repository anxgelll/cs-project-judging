services:
  - type: web
    name: cs-project-judging
    env: docker
    plan: starter
    disk:
      name: grading-data
      mountPath: /var/data
      sizeGB: 1
    envVars:
      - key: DB_PATH
        value: /var/data/grading.sqlite
